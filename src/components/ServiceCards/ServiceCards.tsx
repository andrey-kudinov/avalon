import { ColorSubtitle } from '../ColorSubtitle/ColorSubtitle';
import { SectionTitle } from '../SectionTitle/SectionTitle';

export const ServiceCards = () => (
  <section className="container">
    <ColorSubtitle title="cross-team at the core" />
    <SectionTitle title="We provide services with industry highest expertise and best practices" />
  </section>
);
